# Inference Helpers

`bigru_sentence_model.py` — legacy Knee42 BiGRU 路徑的分類器定義，
由 `realtime/knee42_ivcam.py`、`training/knee42_devonly.py`
與 `evaluation/knee42_test_once.py` 共用。

現行 Transformer 路徑的推論不在這裡，見
[`recognition/transformer/`](../transformer/)。
